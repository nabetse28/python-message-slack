pipeline {

    agent {
        any
    }
    environment {
        BRANCH_NAME = "${GIT_BRANCH}"
    }
    stages {

        stage('Delete Folders & Check Version') {
            steps {        
                           
            }
        }

        stage('Install Dependencies') {
            steps {
                
            }
        }
        stage('Test') {
            steps {
                
            }
        }

        stage('Build') {
            steps {
                                
            }
        }

        stage('Deploy') {
            steps {
                                
            }
        }
    }
}
